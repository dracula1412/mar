- type = @car.type.split(':').last.downcase
%p#notice= notice
%p
  %b
    = "This is #{type} car"

- if @car.working?
  %p 
    This car is working !!
    %br
    = "Time to done: #{time_ago_in_words(@car.ended_at)}"
    - if type == 'worker'
      %p
        = link_to "Speed up", speed_up_worker_car_path(@car), method: :post
- else
  %p
    This car is rest.
    %br
    - if type == 'worker'
      = link_to "Go to work", go_to_work_car_path(@car), method: :post 
    - else
      = link_to "Go to race", maps_path 
